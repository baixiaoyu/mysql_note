back_log:这个选项控制着tcp连接队列的大小，在每秒有很多连接创建销毁的环境中，默认值50过小，会出现偶尔的连接被拒绝的问题。

linux中需要增加somaxconn限制，并检查tcp_max_syn_back_log设置。

mysql 启用skip_name_resove，禁止dns解析

更改本地端口的范围，有效的处理大量连接和肖查询的情况

ip_local_port_range

允许更多的连接队列设置tcp_max_syn_back_log

