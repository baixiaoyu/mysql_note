这个参数指定的内存区域是存放的未提交事务的，这个参数是针对每个会话的，如果事物的大小超过这个参数的指定值，那么数据会放到磁盘上去。可以查看
binlog_cache_use,binlog_cache_disk_use的状态。
sync_binlog代表的是每写缓冲多少次就同步到磁盘，即使这个值设置成了1，也会有问题发生，有下面的情况发生，事物日志部分写入了binlog，但是还没有提交，这个时候宕机了，事物是需要回滚的，这时候就出现问题了，可以通过设置innodb_support_xa=1来设置解决这个问题。

